'use strict';

var FluentClient = require('./FluentClient').default;
var Http = require('./Http').default;
var templates = require('./templates');

module.exports = {
  FluentClient: FluentClient, Http: Http
};